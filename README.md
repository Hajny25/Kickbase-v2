# Kickbase v2

To run: `python3 -m aiohttp.web -H localhost -P 8080 app:init_func`

Assumes the `KICKBASE_USER` and `KICKBASE_PASSWORD` environment variables to be set to the email address and password of a valid Kickbase user.
